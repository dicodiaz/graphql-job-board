type Query {
  company(companyId: ID!): Company
  job(jobId: ID!): Job
  jobs: [Job!]
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  title: String!
  description: String
  company: Company!
}
